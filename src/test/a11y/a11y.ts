/* eslint-disable jest/expect-expect */
describe('Accessibility tests', () => {
  test('placeholder until a11y tests are added', async () => {
    // Replace with pa11y or jest-axe checks against rendered pages
  });
});
